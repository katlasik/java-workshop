package pl.sda.enums;

/**
 * Stwórz typ wyliczeniowy `Color`.
 * Dodaj kolory, takie jak `BLACK`, `RED`, `YELLOW`.
 * Dodaj do niego metodę `String getHex()` zwracającą heksadecymalną reprezentację koloru.
 */

public enum Color {
}
